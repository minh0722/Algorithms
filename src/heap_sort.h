#ifndef ALGORITHM_HEAP_SORT
#define ALGORITHM_HEAP_SORT

#include <vector>
#include "common.h"

namespace algorithm
{
    namespace sort
    {
        ALGO_API void heapSort(std::vector<int>& v);
    }
}

#endif // ALGORITHM_HEAP_SORT